# FitJot プロジェクト仕様

共通の開発ルールは `~/.codex/AGENTS.md` を参照する。本ファイルにはFitJot固有の仕様とv1の範囲を記載する。

## 目的

FitJotは、自宅トレーニングをシンプルに実行・記録するiPhoneアプリ。
細かい筋トレ分析よりも、次に何をやるか迷わないこと、トレーニング中の操作の簡単さ、セット数とインターバルの管理、実施履歴の確認のしやすさを重視する。

## 基本画面

「次回」「履歴」「編集」の3タブ構成とする。

## プランとローテーション

- 曜日固定ではなく「次回」ベースで管理する。
- 初期状態はA/B/Cの3プラン。プランは追加・削除・並び替え可能とする。
- ローテーションはプランの並び順とする。例：A → B → C → A。
- 次回候補とは別のプランを実施した場合、実際に実施したプランを基準に次回候補を決定する。例：次回候補B → Cを実施 → 次回A。

## 次回画面

- 全プラン名を表示し、初期表示では次回候補のプランだけ種目を展開する。
- 別プランをタップすると、そのプランを選択して展開する。
- 主な操作は「トレーニングスタート」と「今日は休養」。
- 休養してもローテーションは進めない。

## 種目と今回の実行値

各種目は以下を持つ。

- 種目名
- 回数制 / 秒数制
- 標準回数または標準秒数
- 標準セット数
- インターバル秒数
- 並び順

実行画面では回数または秒数、セット数、インターバル秒数を変更可能とする。
実行画面での変更は今回だけに適用し、種目の標準値は変更しない。

## 回数制トレーニング

- 「トレーニングスタート」で時間計測を開始する。
- セット進行に応じて「1セット完了」「2セット完了」…「完了」と操作する。
- セット完了後、最終セット以外はインターバルへ移行する。
- 回数制トレーニング中には一時停止 / 再開を設けない。

## 秒数制トレーニング

- 「トレーニングスタート」でカウントダウンを開始する。
- 0秒でセット完了とし、最終セット以外はインターバルへ移行する。
- 秒数制では一時停止 / 再開を可能とする。

## インターバル

- セット間のみ使用し、一時停止・再開・スキップを可能とする。
- 最終セット後はインターバルを開始しない。
- 種目間専用インターバルは設けない。

## スキップ・途中終了

- 実施済みが0セットの種目では「スキップ」と表示する。
- 1セット以上実施済みの種目では「この種目を終了」と表示する。
- 途中で種目を終了しても、実施済みセットはそのまま実績として保存する。
- トレーニング全体も途中終了可能とし、完了済みの内容を保存する。

## 休養

- 次回画面から任意に休養できる。
- 休養設定は「なし」「1ローテーションに1回」「2ローテーションに1回」「3ローテーションに1回」「4ローテーションに1回」。
- 指定ローテーション完了後に休養を提案する。休養は強制しない。

## 編集画面

全プランと種目を表示し、以下を編集可能とする。

- プラン名
- プラン追加 / 削除 / 並び替え
- 種目追加 / 削除 / 並び替え
- 種目名
- 回数制 / 秒数制
- 標準回数 / 秒数
- 標準セット数
- インターバル秒数
- 休養設定
- 通知設定

## 履歴

- 月間カレンダー形式とする。
- トレーニング実施日にアイコンを表示し、休養日は別アイコンで表示する。
- 日付タップで実施内容を表示する。
- 詳細では実施プラン、各種目、回数 / 秒数、実施セット数、スキップ、プラン全体のトレーニング時間を確認できるようにする。
- 各種目の所要時間も内部データとして保持する。
- v1では履歴内容の編集は行わず、削除のみ可能とする。

## 通知

インターバル終了通知を行う。以下を個別に設定可能とし、初期値はすべてONとする。

- 音 ON / OFF
- 振動 ON / OFF
- バックグラウンド通知 ON / OFF

## バックグラウンドと復帰

- タイマーをバックグラウンドで無理に動かし続けない。
- 必要な場合は開始時刻や終了予定時刻から、復帰時に経過時間・残り時間を再計算する。
- アプリ完全終了後のトレーニング状態復元はv1では必須としない。

## 初期データ

初回起動時はA/B/Cの3プランとサンプル種目を用意する。ユーザーは自由に編集可能とする。

サンプル例：

- A：腕立て 10回 × 3セット
- B：プランク 30秒 × 3セット
- C：スクワット 10回 × 3セット

## v1対象外

追加指示がない限り、以下を先回りして実装しない。

- セットごとの回数入力
- 曜日固定スケジュール
- 過去履歴の内容編集
- 複雑なグラフ
- SNS
- AIトレーナー
- 食事 / カロリー / 体重管理
- iCloud同期
- データエクスポート
